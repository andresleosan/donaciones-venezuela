rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Bloqueo inicial. Las rutas privadas y publicas se habilitaran
    // individualmente despues de probar MIME, tamano, rol y propietario.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
