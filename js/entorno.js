// Punto de sobrescritura del backend. En producción este archivo es un stub
// vacío a propósito: `core.js` cae a los valores de Supabase de producción.
//
// El entorno de pruebas sirve este mismo path con un archivo que define
// `window.DV_ENTORNO = { supabaseUrl, supabaseKey }`, apuntando a su propia
// copia de la base. Ese archivo vive fuera del repositorio, así que no puede
// llegar a producción por un merge.
