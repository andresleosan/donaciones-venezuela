{
  "name": "donaciones-venezuela",
  "private": true,
  "version": "0.0.0-migration",
  "type": "module",
  "description": "Plataforma de respuesta humanitaria Venezuela",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:unit": "vitest run --exclude tests/rules/** --exclude tests/emulators/**",
    "test:rules:run": "vitest run tests/rules",
    "test:rules": "firebase emulators:exec --project demo-donaciones-venezuela --only firestore,storage \"npm run test:rules:run\"",
    "test:functions:run": "vitest run tests/functions tests/emulators/health.integration.test.ts tests/emulators/auth.integration.test.ts",
    "test:functions": "npm --prefix functions run build && firebase emulators:exec --project demo-donaciones-venezuela --only auth,functions \"npm run test:functions:run\"",
    "test:emulators:run": "vitest run tests/rules tests/functions tests/emulators",
    "test:emulators": "npm --prefix functions run build && firebase emulators:exec --project demo-donaciones-venezuela --only auth,firestore,storage,functions \"npm run test:emulators:run\"",
    "export:supabase": "node scripts/export-supabase.mjs",
    "export:supabase:dry-run": "node scripts/export-supabase.mjs --dry-run",
    "verify:export": "node scripts/verify-supabase-export.mjs",
    "verify": "npm run test:unit && npm run test:emulators && npm run build && npm audit --audit-level=high && npm audit --prefix functions --audit-level=high && python scripts/verificar-idioma.py"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "fake-indexeddb": "6.2.5",
    "firebase-tools": "15.26.0",
    "vite": "^7.1.7",
    "vitest": "4.1.10"
  }
}
